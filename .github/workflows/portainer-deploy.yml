---
name: Deploy to Portainer

"on":
  push:
    branches:
      - main
    paths:
      - 'stack/**'
      - '.github/workflows/portainer-deploy.yml'
  workflow_dispatch:

jobs:
  trigger-portainer-webhook:
    name: Trigger Portainer GitOps Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Trigger Portainer Webhook
        run: |
          echo "🚀 Triggering Portainer GitOps deployment..."
          curl -X POST \
            https://portainer.ambitiouscake.com/api/stacks/webhooks/f7233af8-8e34-4422-93d2-c89089211dcc
          echo "✅ Portainer webhook triggered successfully"

      - name: Deployment notification
        run: |
          echo "📦 Stack deployment initiated via Portainer"
          echo "🔍 Check Portainer UI for deployment status"
          echo "🌐 Portainer URL: https://portainer.ambitiouscake.com"
